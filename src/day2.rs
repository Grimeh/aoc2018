// input is a list of box IDs (6 letter combination of letters) separated by newlines
// eg. abcdef, aabbcc, ababab, etc.

use std::collections::HashMap;

// calculate a "checksum" of all the box IDs
fn d2p1(input: &str) -> u64 {
	// let map = HashMap::new();

	// for line in input.lines() {

	// }

	return 0;
}